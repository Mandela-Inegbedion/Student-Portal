<!DOCTYPE html>
<html>
<head>
  <title>Admission Status</title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <div class="form-container">
    <h2>Admission Status</h2>
    <% if (status === 'admitted') { %>
      <p class="success">Congratulations! You have been admitted.</p>
    <% } else if (status === 'not admitted') { %>
      <p class="error">Admission has not been granted.</p>
    <% } else { %>
      <p class="pending">Your admission status is still pending.</p>
    <% } %>
  </div>
</body>
</html>
